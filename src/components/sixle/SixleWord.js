import './SixleWord.scss'
import { propTypes } from 'react-bootstrap/esm/Image'

const SixleWord = ({ meta }) => {
  return (
    <div className="Word">
    </div>
  )
}

SixleWord.propTypes = {
  meta: propTypes.array
}

export default SixleWord